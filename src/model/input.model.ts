export type InputData = [number[], number[]]; // x[], y[]
export type Input = {
    data: InputData,
    N: number,
    S: number,
    P: number,
    downsampleAt: number,
    // newConfiguration: boolean,
}