export type InputData = { x: number; y: number }[];
export type Input = {
    data: InputData,
    N: number,
    S: number,
    P: number,
    downsampleAt: number,
    // newConfiguration: boolean,
}